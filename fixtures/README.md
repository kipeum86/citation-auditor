# Fixtures

`fixtures/` contains clean markdown reports and live slash-command validation fixtures.

`fixtures/seeded/` contains parallel versions with three deliberately wrong citation-bearing or factual claims inserted for Week 1 testing.

`v1.2-global-legal.md` is the live slash-command fixture for the US/UK/EU verifier expansion.

`v1.4-docx-legal.docx` is the live slash-command fixture for DOCX mode. Expected outcomes and manual E2E pass criteria are recorded in `v1.4-docx-legal.expected.md`.
