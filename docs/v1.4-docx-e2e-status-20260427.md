# v1.4 DOCX E2E Status - 2026-04-27

## Current Status

상태: artifact review 통과, chat transcript 확인 대기.

사용자가 실제 Claude Code slash-command 실행은 별도 Claude 세션에 맡겼다. 이 Codex 세션에서는 repo에 생성된 sidecar 산출물만 검토했다. 따라서 아래 항목은 "산출물 기준 통과"로 기록하고, 전체 E2E 통과 선언은 Claude 세션의 채팅 응답/trace 확인 뒤에만 한다.

## Observed Artifacts

- Fixture: `fixtures/v1.4-docx-legal.docx`
- Expected file: `fixtures/v1.4-docx-legal.expected.md`
- Observed sidecars:
  - `fixtures/v1.4-docx-legal.audit.md`
  - `fixtures/v1.4-docx-legal.audit.json`
- Generated timestamp in report: `2026-04-27T06:08:38Z`
- Report mode: `external_report`

The generated sidecars are runtime outputs and are intentionally ignored by git. They may contain machine-local temp paths in `source.markdown_path`, so they should not be committed as stable fixtures.

## Artifact Review

| Check | Result | Evidence |
|---|---|---|
| DOCX sidecar markdown exists | Pass | `fixtures/v1.4-docx-legal.audit.md` observed |
| DOCX sidecar JSON exists | Pass | `fixtures/v1.4-docx-legal.audit.json` observed |
| Summary counts match expected | Pass | `contradicted: 3`, `unknown: 0`, `verified: 4`, `total: 7` |
| Three deliberate false claims contradicted | Pass | 민법 제2000조, 개인정보 보호법 제88조 pairing, 2023 게임산업 매출 15% 증가 |
| Expected verified claims verified | Pass | 민법 제103조, GDPR Article 17, 개인정보 보호법 제15조, EU AI Act 2024 |
| Forecast/speculation skipped | Pass | `업계 관계자에 따르면 AI 규제는 곧 완화될 전망이다` absent from findings |
| Table-cell locations preserved | Pass | `표 1 / 행 1 / 열 1`, `표 1 / 행 1 / 열 2`, `표 1 / 행 2 / 열 1`, `표 1 / 행 2 / 열 2` present |
| Machine-readable report schema present | Pass | JSON has `summary`, `scope`, `findings` |
| Original DOCX not modified | Pass | `git status` did not report `fixtures/v1.4-docx-legal.docx` as modified |

## Still Needed From Claude E2E Run

Claude 세션에서 다음 2가지만 확인되면 full E2E pass로 기록할 수 있다.

1. 실제 실행 명령이 Python component command가 아니라 slash command였는지:

   ```text
   /citation-auditor:audit fixtures/v1.4-docx-legal.docx
   ```

2. 채팅 응답이 보고서 전문을 붙여넣지 않고 `.audit.md` / `.audit.json` 경로와 간단한 summary만 반환했는지.

## Changelog Policy

Claude transcript 확인 전에는 `CHANGELOG.md`에 "full real slash-command E2E passed"라고 쓰지 않는다. 현재 표현은 "sidecar artifact review passed; chat transcript confirmation pending" 수준으로 유지한다.

## Vendor Decision

각 legal agent의 vendored copy는 지금 전면 업데이트하지 않는다.

Current decision:

- Global/plugin path만 쓰는 프로젝트는 본체 업데이트로 충분하다.
- 기존 markdown 감사가 안정적으로 동작 중인 legal agent는 v1.3 vendored copy를 유지한다.
- DOCX 직접 감사가 실제로 필요한 repo 1개만 canary로 업데이트한다.
- Canary도 Claude transcript 확인 뒤에 진행한다.

Canary 조건:

- 해당 repo에 실제 DOCX 감사 수요가 있다.
- `.claude/skills/citation-auditor/`에 repo별 local customization이 없거나, 덮어쓰기 전 diff를 확인했다.
- 먼저 dry run을 실행한다.

  ```bash
  ./scripts/vendor-into.sh --dry-run /path/to/canary-repo
  ```

- v1.3 vendored copy 위에 적용할 때는 명시적으로 확인 플래그를 사용한다.

  ```bash
  ./scripts/vendor-into.sh --confirm-docx-upgrade /path/to/canary-repo
  ```

- canary repo 안에서 markdown fixture와 DOCX fixture를 모두 실제 slash command로 검증한다.
- 며칠 사용 후 문제가 없으면, 나머지는 필요할 때만 순차 업데이트한다.
